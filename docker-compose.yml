version: '3.8'

services:
  # TIER 1: THE FRONTEND USER INTERFACE LAYER
  frontend:
    image: priyazinzu/portfolio-frontend:latest
    container_name: portfolio_frontend
    ports:
      - "8080:80"
    networks:
      - portfolio_net
    restart: always

  # TIER 2: THE BACKEND APPLICATION LOGIC LAYER
  backend:
    image: priyazinzu/portfolio-backend:latest
    container_name: portfolio_backend
    ports:
      - "8000:80"
    networks:
      - portfolio_net
    depends_on:
      - db
    restart: always

  # TIER 3: THE DATABASE STORAGE LAYER
  db:
    image: mysql:8.0
    container_name: portfolio_database
    environment:
      MYSQL_ROOT_PASSWORD: root_secure_pass
      MYSQL_DATABASE: portfolio_db
      MYSQL_USER: portfolio_user
      MYSQL_PASSWORD: portfolio_secure_pass
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - portfolio_net
    restart: always

# CUSTOM VIRTUAL NETWORK DEFINITION
networks:
  portfolio_net:
    driver: bridge

# PERSISTENT STORAGE VOLUME DEFINITION FOR MYSQL
volumes:
  db_data:
    driver: local
