FROM nginx:alpine

# Copy all local assets (index.html, resume.pdf) into Nginx's default public directory
COPY . /usr/share/nginx/html

# Expose port 80 internally inside the container network grid
EXPOSE 80
